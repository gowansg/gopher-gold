<%= form_for(@answer_choice) do |f| %>
  <% if @answer_choice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@answer_choice.errors.count, "error") %> prohibited this answer_choice from being saved:</h2>

      <ul>
      <% @answer_choice.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %><br>
    <%= f.text_field :question %>
  </div>
  <div class="field">
    <%= f.label :id %><br>
    <%= f.text_field :id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
